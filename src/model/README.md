# Model

The rail. It does not draw. Cards declare deltas. `engine.ts` applies them.

Chair ids are the Train Ride fork: `us` is Washington, `iran` is the Street. Relabelled in the view.

The book is the `irgc` bar. Soft plus book under 35, or book under 20, is seizure.

Hindsight overlay scores an offramp and does not steal the rail.
